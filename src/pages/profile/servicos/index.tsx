import React from 'react';
import { Panel, Header, Input } from '../../../components/UI-Elements';

const Servicos = () => {
  return (
    <div>
      <Panel>
        <Header>Serviços</Header>
      </Panel>
    </div>
  );
};

export default Servicos;
